import 'package:flutter/material.dart';

class WcRequestModal extends StatelessWidget {
  const WcRequestModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog(
      child: SizedBox.shrink(),
    );
  }
}
